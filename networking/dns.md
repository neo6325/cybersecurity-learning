# 🌐 DNS (Domain Name System)

DNS stands for **Domain Name System**.

It translates human-readable domain names into IP addresses so that devices can locate services on a network.

For example:

```text
www.example.com
        ↓
    DNS lookup
        ↓
93.184.216.34
